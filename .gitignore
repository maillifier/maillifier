# Claude Code project settings (local dev tool, not part of the project)
.claude/

# clasp local config (contains Script ID — set by CI)
.clasp.json

# CI/CD workflows (managed in private maillifier.dev repo)
.github/
